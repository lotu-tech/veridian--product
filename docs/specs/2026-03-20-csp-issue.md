# Feature: Emergency CSP Fix - Unblock API Domain

## Summary
Fix Content Security Policy blocking api.veridianosint.com.br, restoring platform functionality for all users. Recent security PR introduced CSP rules that block the app's own API calls.

## User Journeys
1. User logs in to app.veridianosint.com.br with valid credentials
2. App makes API calls to api.veridianosint.com.br (e.g., /users/list-credits-money)
3. Browser allows the requests instead of blocking with CSP violation
4. User can access dashboard and all platform features normally

## Acceptance Criteria
- [ ] API calls from app.veridianosint.com.br to api.veridianosint.com.br succeed
- [ ] CSP console errors are resolved
- [ ] Users can log in and access all platform functionality
- [ ] No regression in security posture beyond intended API domain allowance

## Scale & Volume
All platform users affected (production outage). API calls happen continuously during normal app usage.

## Affected Projects
- investigation-front: Update CSP headers/meta tags to whitelist api.veridianosint.com.br domain

## Constraints
CRITICAL: Production outage requiring immediate hotfix and deployment.